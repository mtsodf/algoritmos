{
    "configurations": [
        {
            "name": "(ctest) Launch",
            "type": "cppdbg",
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "program": "${cmake.testProgram}",
            "args": [
                "${cmake.testArgs}"
            ],
            "MIMode": "lldb"
            // other options...
        }
    ],
    "version": "2.0.0"
}